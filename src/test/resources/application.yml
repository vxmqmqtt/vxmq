vxmq:
  broker:
    enabled: false
    # Test profile keeps the empty auth chains open; production defaults must not set no-match allow.
    authn:
      no-match: allow
    authz:
      no-match: allow

quarkus:
  micrometer:
    binder:
      netty:
        enabled: false
  kafka:
    devservices:
      enabled: false
  devservices:
    enabled: false
