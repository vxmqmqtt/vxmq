vxmq:
  broker:
    enabled: false
    authn:
      no-match: allow
    authz:
      no-match: allow

quarkus:
  micrometer:
    binder:
      netty:
        enabled: false
  kafka:
    devservices:
      enabled: false
  devservices:
    enabled: false
