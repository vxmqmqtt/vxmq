# 协作与贡献约定

本文档定义项目当前有效的协作与贡献规则。它不是历史记录；若某条规则背后需要解释原因，应补充 ADR，而不是把历史背景混入本文。

## 基本约定

- 默认使用中文沟通和撰写项目内文档。
- 涉及 MQTT 语义时，以 OASIS MQTT 3.1.1 与 MQTT 5 规范正确性优先，不以实现便利替代规范要求。
- 对不确定信息必须显式标注为“已确认”“待确认”或“假设”。
- 发现当前方案会造成明显返工时，应直接指出风险并给出替代方案。

## 工作流程

- 开始实现前，先明确目标、边界、输入输出和验收标准；目标不清时先澄清。
- 小改动可以直接实施，但完成后必须说明原因、影响范围和验证结果。
- 大改动应先给方案再实施；方案应包含取舍、测试策略和文档影响。
- 每项重要功能都应同步考虑测试策略与验收方式，避免先写实现、后补验证。
- 阶段性工作完成后，应说明结果、影响范围、验证情况和建议的下一步。

## 文档契约

- 新增文档前先判断是否已有合适位置，避免重复维护。
- 文档标题应直接表达主题，避免“设计说明一”“方案草案二”这类弱命名。
- 文档内容若依赖假设条件，必须在文首或相关章节明确说明。
- 当前状态只维护在 `docs/01-status/`。
- 长期系统真相只维护在 `docs/00-foundation/` 和 `docs/02-system/`。
- ADR 只记录关键决策原因，统一放在 `docs/03-decisions/`，不承担当前状态入口职责。
- 过期文档和被替换的旧专题文档进入 `docs/99-archive/`，不作为当前真相引用源。
- 修改文档结构后，必须同步检查 `README.md`、`docs/README.md` 和相关入口链接。
- 阶段推进、封板或进入新里程碑时，必须同步检查 `docs/01-status/current-status.md`、`docs/01-status/roadmap.md` 和 `docs/01-status/mqtt5-feature-matrix.md`。
- 对架构分层、状态流转、协议时序和复杂决策路径，应优先使用 Mermaid 图辅助表达；图必须服务于澄清结构，不为形式重复正文。

## 开发与验证

- 优先构建可验证的最小闭环，再扩展高级能力。
- 优先可验证实现，再做性能优化。
- 对关键行为建立自动化测试，不依赖手工验证或记忆。
- 变更应尽量局部化；需求尚不明确时，避免大范围重构。
- 主链路遵循响应式、非阻塞、event-loop-first 模型；若需要引入阻塞操作，必须先显式设计边界并记录原因。
- 在 Quarkus 中直接接入 Vert.x 扩展时，应先检查是否存在 Mutiny 变体；若存在，默认优先使用 Mutiny 变体。
- 新增或修改生产代码、测试代码时，应补充有信息量的注释；公共类型、接口和关键方法使用简洁 Javadoc 说明职责、核心输入输出或协议语义。
- 复杂状态转换、协议分支、并发处理和回滚逻辑应说明“为什么这样做”；禁止写“给变量赋值”“调用方法”这类低信息量注释。
- 测试注释应说明“验证什么行为、针对什么场景”；若一个测试覆盖多个场景，应优先拆分为多个清晰测试。

## 协议决策

- 新增设计或实现不得默认只面向 MQTT 3.1.1 或 MQTT 5，除非文档明确说明阶段性限制。
- 对协议未规定、允许多种实现或存在 `MAY` 行为的地方，实现前应先核对 OASIS 规范。
- 当规范不足以给出唯一结论时，可参考 HiveMQ、EMQX、Mosquitto 等同类产品的官方资料或可重复实测行为。
- 一旦项目采纳某种协议选择，应记录到 ADR 或稳定设计文档中。

## 沟通规则

- 讨论问题时优先给出结论、依据和风险，而不是只给观点。
- 存在多个可行方案时，应说明取舍，不默认把复杂方案当成更优方案。
- 需要用户确认时，应把问题收敛成少量关键决策点。
